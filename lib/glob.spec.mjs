import glob from "./glob.mjs";

// Currently `host/builtins.spec.mjs` tests globs using the `findstr` and `glob` builtins.

// And `main.spec.js` tests toRegExp via the "host should handle globs" test cases.